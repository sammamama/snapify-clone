export type BackgroundType ={
  name: string,
  class: string
}

export type Theme = {
  id: string;
  name: string;
  themeHref: string;
  preview: string;
  textColor: string;
};

export type languageType = {
    name: string;
    placeholder?: string;
  };